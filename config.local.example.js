/* =====================================================================
   Cook-Pilot / CookMate — example key file.
   ---------------------------------------------------------------------
   COPY this file to `config.local.js` (which is git-ignored) and paste
   your real Gemini key there. Never commit your real key.

       cp config.local.example.js config.local.js   # then edit the key

   Get a free key: https://aistudio.google.com/apikey
   ===================================================================== */
window.COOKPILOT_GEMINI_KEY = "PASTE_YOUR_GEMINI_KEY_HERE";
